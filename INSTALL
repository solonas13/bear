Pre-installation Instructions
=============================

   This installation requires the pre-installation of the cmake tool,
library zlib, and a C++ compiler.

For Linux, you can install all other required libraries via

 $ ./pre-install.sh 

Basic Instructions
==================

   The shell command `make -f Makefile' should compile this program. 
   
After compilation the binary `bear' will be created in the working 
directory, e.g. you may call it from this directory via

 S ./bear -h
